name: Bug report
description: Something in Data Doctor is wrong or confusing
title: "[Bug] "
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        Prefer **About → Report issue** in the app (GitHub device-code sign-in files
        the issue as you, with an optional log tail). This form is for filing from the website.
  - type: input
    id: version
    attributes:
      label: Data Doctor version
      placeholder: "3.0.0 (About window)"
    validations:
      required: true
  - type: input
    id: os
    attributes:
      label: OS
      placeholder: "Windows 11 / Ubuntu 24.04 / macOS …"
    validations:
      required: true
  - type: dropdown
    id: install
    attributes:
      label: How did you install it?
      options:
        - Windows launcher zip (Data Doctor.exe)
        - Linux AppImage
        - macOS Data Doctor.command
        - From source
        - Other
  - type: textarea
    id: what
    attributes:
      label: What happened
      description: What you did, what you expected, what you got.
    validations:
      required: true
  - type: textarea
    id: log
    attributes:
      label: app.log tail (optional)
      render: shell
