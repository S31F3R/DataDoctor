TEST FILE — currently being worked / needing retest
============================================================
Workflow:
  - To Do List.txt = backlog only
  - This file = in-progress / Seifer tests here
  - PASS → mark [x] here (stay off To Do)
  - FAIL → note here; Grok re-fixes


Need retest (this pass)
--------------------------------------------------------------------------------

1) Aquarius Label = Label only (no location /n line)
   Steps: Internal Aquarius → cell Show details.
   EXPECT: first row "Aquarius Label" shows only the series Label text (single line).
           LocationIdentifier is NOT under it.
   Notes: 2026-07-23 kinda fixed with Label+location; removed location line.

2) Taskbar / app icon
   Steps: Launch Data Doctor (Linux panel / Windows taskbar).
   EXPECT: app uses DataDoctor.ico (or Data Doctor.png fallback), not a generic Python icon.
   Notes: app.setWindowIcon + winMain.setWindowIcon at startup.

3) Wrong HDB/Oracle password does not spam-lock account
   Steps: Set deliberately wrong Oracle password in Options → Save → run internal HDB query
          (or SQL Builder against HDB). Optionally multi-series / multi-DB.
   EXPECT:
     - One clear "Oracle Login Failed" style message (wrong user/password or locked).
     - Further connects blocked until you save correct credentials in Options again.
     - Not a storm of failed login attempts from worker threads.
   Notes: OracleAuthError + process-wide auth fail-fast; cleared on credential save.


Confirmed (do not re-break)
--------------------------------------------------------------------------------
[x] About secret π icon (clean glyph)
[x] Header Max/Min show timestamps
[x] HDB r_base fallback coloring
[x] Export CSV uses active tab
[x] HDB headers = commonName - datatype
[x] Details max 16 rows + themed scrollbar
[x] Overlay/HDB no ghost 3rd column
[x] USGS meta no duplicate rows / USGS-only meta
[x] USGS headers use Site Name (comma split)
[x] Save Quick Look dialog wider
[x] Query window wider (USGS UID)
[x] Raw data deltas without scientific notation
[x] End timestamps exclusive end
[x] BOP/EOP Options radios
[x] Header sort / Undo preserves Overlay
[x] QAQC vs Overlay order
[x] Password paste while masked
[x] Export complete message


Optional smoke
--------------------------------------------------------------------------------
[ ] Large dataset warning removed (>10k Yes/No gone)
[ ] Header sort not stacked (sectionClicked once)
