TEST FILE — currently being worked / needing retest
============================================================
Workflow:
  - To Do List.txt = backlog only
  - This file = in-progress / Seifer tests here
  - PASS → mark [x] here (stay off To Do)
  - FAIL → note here; Grok re-fixes


Need retest (this pass)
--------------------------------------------------------------------------------

1) Large multi-source query does not crash on Oracle PATH (Windows)
   Steps: Internal query ~2 years, many series across USGS + USBR-LCHDB + USBR-YAOHDB
          + Aquarius (same shape that failed before).
   EXPECT: No ValueError "environment variable is longer than 32767 characters".
           Query completes or fails cleanly without process crash mid-Oracle setup.
   Notes: 2026-07-24 FAIL — every oracleConnection() prepended Instant Client to PATH;
          multi-thread HDB (SDID × date chunks) blew past Windows 32k env limit.
          FIX: ensureOracleClientReady() once per process; PATH prepend only if missing.
          2-day control still OK (few tasks → PATH never hit the ceiling).

2) Data Dictionary vertical scrollbar grip usable with ~40k rows
   Steps: Open Data Dictionary (full load).
   EXPECT: vertical scrollbar handle is thick enough to grab (min-height ~48px, track ~20px).


Confirmed recently
--------------------------------------------------------------------------------
[x] Tab order main + query
[x] Options remask secrets on close
[x] SQL Query Builder background thread
[x] Aquarius Label = Label only
[x] (prior bug-smash cluster — export, headers, meta, rbase, auth, etc.)


Deferred / external
--------------------------------------------------------------------------------
[!] Windows taskbar icon when launched via VB.NET → Python
[!] USGS monthly/yearly — OGC has continuous + daily only
[!] Retro restart Windows / Windows text truncation
[!] Aquarius daily 2300 TZ
[!] Features: plots, log viewer, goat, formatting rules, git/readme
