TEST FILE — tests to run
============================================================
Grok writes the steps. Mark [x] when it passes, or note a fail.
Workflow / publish how-to: documentation/status.grok (gitignored).
“see updates” = collapse [x], handle every RESPONSE, do To Do CURRENT TASKS/BUGS.
RESPONSE: under a test = Seifer ran it and left notes. Grok must act on those.
Current Tests: untested / failed / retest. Passed [x] items collapse to Confirmed.
Confirmed: tests passed since the last published X.Y.Z. A published
publishRelease.py folds any titles missed in the change list, then clears this.

Current Tests
--------------------------------------------------------------------------------
[ ] Mac package script
   Steps:
     a. From project root: python scripts/packageMac.py --skip-venv
     b. dist/DataDoctor-macOS-YYYYMMDD.zip contains Data Doctor.command + pythonFiles/
        + updates/ + applyUpdate.sh + pythonFiles/certs/
     c. On a Mac: unzip, double-click Data Doctor.command (needs Python 3.13 / venv).
     d. Optional on Mac only: python scripts/packageMac.py --app → .app via PyInstaller.

[ ] AppImage update + bunker (Linux AppImage install)
   Steps:
     a. Rename the AppImage (e.g. MyDoctor.AppImage) and run it from Downloads.
        Confirm ~/.config/Data Doctor/bunker.db exists and dictionary saves persist.
     b. In-app update → Quit and apply. App quits, replaces MyDoctor.AppImage
        (same name), relaunches. Downloads still has that one AppImage — no
        updates/ folder, no applyAppImageUpdate.sh, no .bak beside it.
     c. If packaged bunker differs: Common Name / Data Type prompts. N still adds new IDs.
     d. If apply fails: ~/.config/Data Doctor/logs/applyUpdate.log has the updater output.

[ ] Background update check
   Steps:
     a. Launch the app. Startup check still runs (same Update available prompt).
     b. Click Later. No second prompt this session (leave the app open; no need to wait 4 hours).
     c. Quit and relaunch — update prompt can appear again.

[ ] Options Appearance table fits
   Steps:
     a. Options → Appearance. Overlay primary/secondary flag rows are fully visible.
     b. Restore Defaults sits below the table, not on top of "Uploaded this session".
     c. No vertical scroll needed for that table.

[ ] Overlay upload same-SDID
   Steps:
     a. Overlay pair, fill secondary into primary, Upload many timestamps of one SDID.
     b. No ORA-00001 IDX_REF_CHANGE_AGENT_LOOKUP. app.log shows one series per worker.

Confirmed
--------------------------------------------------------------------------------
Formula leading decimal
Quick Look dirty prompt
Data Dictionary save confirmation
About version link
Graph legend ASCII marks
Query option flags + overlay colors
Equation in query list / Quick Look
Bunker merge N vs new rows
Overlay flag colors in Options
Feature request in-app form
Log Viewer colors survive Options Save
SDID-MRID upload uses MODIFY_M_TABLE

