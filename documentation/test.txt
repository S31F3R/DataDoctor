TEST FILE — currently being worked / needing retest
============================================================
Workflow:
  - To Do List.txt = backlog only
  - This file = in-progress / Seifer tests here
  - PASS → mark [x] here (stay off To Do)
  - FAIL → note here; Grok re-fixes


GATE before staging push
--------------------------------------------------------------------------------

V1) Large multi-source “Building table…” must finish (not hang)
   Steps: 2-year mega query (USGS + multi-HDB + Aquarius, many series) as before.
          After fetch completes, watch progress during Building table / QAQC.
   EXPECT:
     - Progress label updates: "Building table... (row/total rows, N cols)"
       then "Applying QAQC colors..." if QAQC on.
     - Windows does NOT stay "Not Responding" forever; UI yields during fill.
     - Table eventually appears (minutes may still be OK for ~1M+ cells; hang/crash is fail).
   Notes:
     2026-07-24 PATH crash FIXED; then build hung ("program is not responding").
     Root causes of hang:
       - Vertical header ResizeToContents on every row (Qt scans all rows)
       - Million+ QTableWidgetItem inserts with rare/no processEvents
       - Re-enabling updates mid-fill forced expensive paints
       - QAQC + disabled-QAQC paths walked every cell; dict lookup was O(rows) per series
     FIX (retest this):
       - No ResizeToContents on large fills
       - processEvents every ~200–1000 rows while updates stay OFF
       - Live progress % 90–98 during cell fill
       - O(1) data-dictionary index; QAQC once after overlay/delta only
       - Skip useless full-table background clear when QAQC off


Confirmed recently
--------------------------------------------------------------------------------
[x] Data Dictionary scrollbar thick + retro theme
[x] Large multi-source query: no Oracle PATH 32767 crash
[x] Tab order main + query
[x] Options remask secrets on close
[x] SQL Query Builder background thread
[x] Aquarius Label = Label only
[x] (prior bug-smash cluster)


Deferred / external
--------------------------------------------------------------------------------
[!] Oracle HDB speed after staging (C connection reuse first)
    See documentation/oracle-speed-options.txt
[!] Windows taskbar icon via VB.NET launcher
[!] USGS monthly/yearly — OGC continuous + daily only
[!] Retro restart Windows / Windows text truncation
[!] Aquarius daily 2300 TZ
[!] Features: plots, log viewer, goat, formatting rules, git/readme
