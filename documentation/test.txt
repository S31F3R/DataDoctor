TEST FILE — currently being worked / needing retest
============================================================
Workflow:
  - To Do List.txt = backlog only
  - This file = in-progress / Seifer tests here
  - PASS → mark [x] here (stay off To Do)
  - FAIL → note here; Grok re-fixes


Need retest (this pass) — Oracle option A: batch SDIDs
--------------------------------------------------------------------------------

1) Multi-series HDB IN-list batching (with connection reuse from C)
   Steps:
     a. Internal query with many HDB series on one DB (e.g. 20–50+ LCHDB series),
        yesterday→now or multi-day HOUR (same as daily use is fine).
     b. Optional debug log: "batch of N SDIDs", "interval IN-list query n=N",
        fewer batch tasks than (#series × #chunks).
   EXPECT:
     - Values match prior runs (spot-check a few SDIDs).
     - Context-menu meta / r_base still works on HDB cells.
     - Faster than single-SDID SQL when many series (fewer round-trips).
     - Connection reuse still: one open/close per worker, N batch tasks.
   Notes: sdidBatchSize=25; WHERE site_datatype_id IN (:1..:n) + date range.


Confirmed
--------------------------------------------------------------------------------
[x] Oracle option C — connection reuse per worker
      2026-07-24 works great (HDB ~60s slower than USGS/Aquarius on normal day query)
[x] V1 large table build + overlay
[x] PATH Instant Client env limit
[x] Data Dictionary scrollbar retro
[x] Prior bug-smash cluster


Deferred
--------------------------------------------------------------------------------
[!] Oracle D pool (optional; C covers per-sqlRead reuse — pool if multi-query later)
[!] Windows taskbar via VB launcher
[!] USGS monthly/yearly
[!] Retro restart Windows / Windows text
[!] Aquarius daily 2300 TZ
[!] Features: plots, log viewer, etc.
