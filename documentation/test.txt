TEST FILE — tests to run
============================================================
Grok writes the steps. Mark [x] when it passes, or note a fail.
Workflow / publish how-to: documentation/status.grok (gitignored).
“see updates” = collapse [x], tend RESPONSE, do To Do CURRENT TASKS/BUGS.
Current Tests: untested / failed / retest. Passed [x] items collapse to Confirmed.
Confirmed: tests passed since the last published X.Y.Z. A published
publishRelease.py folds any titles missed in the change list, then clears this.

Current Tests
--------------------------------------------------------------------------------
[ ] Mac package script
   Steps:
     a. From project root: python scripts/packageMac.py --skip-venv
     b. dist/DataDoctor-macOS-YYYYMMDD.zip contains Data Doctor.command + pythonFiles/
        + updates/ + applyUpdate.sh + pythonFiles/certs/
     c. On a Mac: unzip, double-click Data Doctor.command (needs Python 3.13 / venv).
     d. Optional on Mac only: python scripts/packageMac.py --app → .app via PyInstaller.

[ ] Uncheck Beta on published — no dialog
   Steps:
     a. Install is a published X.Y.Z (not -rc / -beta), Beta updates currently checked.
     b. Options → uncheck Beta updates → Save.
     c. No popup (not “no published release” and not “Revert to published”).
     d. Recheck Beta → Save: update check may offer the newest rc/beta as usual.

[ ] Profile Data Dictionary
   Steps:
     a. Options → Export: Data Dictionary is checked with the other items.
        Zip contains dataDictionary/bunker.db.
     b. Import a zip that includes it: live bunker.db is replaced (no
        commonName/datatype prompts). Data Dictionary window shows the imported rows.

[ ] Bunker merge threads + Merging status
   Steps:
     a. Run a dictionary merge (applyUpdate or updateBunker.py). After
        Backup: path, console shows Merging... before Merge complete.
     b. Merge still updates siteName/database, prompts commonName/datatype,
        fills blanks only, inserts packaged-only rows.

[ ] SQL result column filters
   Steps:
     a. Run a SQL query. Each result header has a filter icon. Column with
        ≤10 unique values: list including (All). More than 10: text box.
     b. Filter hides non-matching rows. Right-click icon → Clear filter.
     c. New extract on that tab (or another result tab) resets filters.
        Pinned tab keeps its own filters; a new extract on another tab
        does not reuse them.
     d. Header text is fully visible — the filter icon sits to the right
        without covering the label (narrow columns grow).
        RESPONSE: Filters work but the column sizing doesn't take into account the filters, so at times the filter overlaps the text

[ ] Filter click-out cancels
   Steps:
     a. Data Dictionary / Search / SQL: open a text or list filter, type or
        highlight something, click outside (do not press Enter / pick).
        Popup closes; the previous filter (or none) is unchanged.
     b. Escape also closes without applying. Enter / a list pick still applies.

[ ] New-setup Add Data Type defaults
   Steps:
     a. Fresh user.config (or first-run defaults): Options → Add Data Type
        to Labels is checked for USBR and USGS, unchecked for Aquarius.
     b. Existing saved config is left as the user last saved it.

[x] Header Remove column
   Steps:
     a. Overlay on: header Remove on the overlay column drops the pair from
        the table and the query list. With Delta on, overlay + delta go.
     b. Overlay off, Delta on: Remove on any of the three columns drops all 3.
     c. Insert column: Remove drops only that custom column. Query series stay.
     d. Highlight 2+ columns, right-click Remove on one of them: all
        highlighted columns go (overlay/delta groups still expand with them).
     RESPONSE: Works when I only try to remove one. If I hightlight 2, it only removes the one I right click on. Overlay and delta logic works correctly

[ ] Multi-column header drag
   Steps:
     a. Shift- or Ctrl-select three columns, then drag one of them: all three
        stay selected and insert at the drop marker (overlay/delta groups
        still travel together).
     b. Overlay + delta still move as a pair when dragging one of them.
     c. After the drop, the highlight is on the columns in their new
        place (not left behind at the old slots).
     RESPONSE: Not sure how long this bug has been in there but when the columns move, they move correctly but the hightlight area stays the location they WERE in, not the new location. On the column move logic, keep the highlight with the column, not the location of where it started

[ ] GitHub update check blocked
   Steps:
     a. With GitHub unreachable (blocked/timeout): startup or update check
        shows “Unable to check GitHub for updates.” (not silent, not “already
        current”).
     b. When GitHub works and you are current: still no popup.

Confirmed
--------------------------------------------------------------------------------
Packaged Quick Looks adopt then remove
About Report issue → Form
About Feature request Form
Graph no axis titles; Y ticks match line
SQL dialogs parent to SQL window

Deferred / known
--------------------------------------------------------------------------------
[!] AppImage applyUpdate path not wired the same as launcher packages yet
[!] Rebuild Data Doctor.exe on Windows to ship launcher VB first-run extract message

