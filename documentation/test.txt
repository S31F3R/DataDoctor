TEST FILE — currently being worked / needing retest
============================================================
Workflow:
  - To Do List.txt = backlog only
  - This file = in-progress / Seifer tests here
  - PASS → mark [x] here (stay off To Do)
  - FAIL → note here; Grok re-fixes


Need retest (this pass) — Oracle connection reuse (option C)
--------------------------------------------------------------------------------

1) HDB multi-task reuse: fewer logins, still correct data
   Steps:
     a. Internal query with several HDB series over a multi-day range (enough to
        create multiple SDID × chunk tasks — e.g. week+ of HOUR, or 2-year mega).
     b. Optionally watch debug log: each worker should log "opened reusable
        connection" once and "closed connection after N task(s)" with N > 1.
   EXPECT:
     - Results match previous behavior (values, meta/details still work).
     - Wall time lower than connect-per-task (esp. over WAN); no auth spam.
     - Wrong password still fails fast (one clear login error).
   Notes: sqlRead workers open 1 Oracle session each, run many tasks, then close.
          executeCustomQueryWithRetry reconnects once on dropped connections.


GATE / already confirmed (staging)
--------------------------------------------------------------------------------
[x] V1 large multi-source build table + overlay/delta finishes (not hang)
[x] PATH Instant Client env limit
[x] Data Dictionary scrollbar retro
[x] Prior bug-smash cluster


Deferred
--------------------------------------------------------------------------------
[!] Oracle A batch SDIDs + D pool (after C validated)
[!] Windows taskbar via VB launcher
[!] USGS monthly/yearly — OGC continuous + daily only
[!] Retro restart Windows / Windows text
[!] Aquarius daily 2300 TZ
[!] Features: plots, log viewer, etc.
