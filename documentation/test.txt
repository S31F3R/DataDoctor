TEST FILE — currently being worked / needing retest
============================================================
Workflow:
  - To Do List.txt = backlog only
  - This file = in-progress / Seifer tests here
  - PASS → mark [x] here (stay off To Do)
  - FAIL → note here; Grok re-fixes


Need retest (this pass)
--------------------------------------------------------------------------------

1) About secret π icon loads
   Steps: Open About. Look bottom-right corner for the Net π glyph.
   EXPECT: small visible π icon (not blank). Uses ui/icons/pi.png crop, not the full still.
   Notes: 2026-07-23 FAILED — full pi.jpg scaled to 16px looked empty.
          Re-fix: cropped glyph → pi.png + QPixmap scale to 20px.

2) USGS headers use Site Name (comma split)
   Steps:
     a. Public or internal OGC USGS query (Site-time_series_id-Parameter).
     b. Check column header after data loads.
   EXPECT:
     - Site Name from API, e.g. "COLORADO RIVER NEAR CISCO" on line 1,
       "UT" (or second comma segment) on line 2 — not site-param only.
     - Meta still available on internal (right-click Show details).
     - Public: headers still use Site Name; no cell context menu (unchanged).
   Notes: labelsDict gets Site Name for USGS; buildTable prefers it; seriesResponses
          stay keyed by DataID so details still resolve.


Confirmed this session (do not re-break)
--------------------------------------------------------------------------------
[x] Export CSV uses active tab
[x] HDB headers = commonName - datatype
[x] Details max 16 rows + themed scrollbar
[x] Overlay/HDB no ghost 3rd column
[x] USGS meta no duplicate rows
[x] USGS meta on USGS-only / non-overlay
[x] Save Quick Look dialog wider (~+60px)
[x] Query window wider (USGS UID)
[x] Raw data deltas without scientific notation
[x] End timestamps exclusive end
[x] BOP/EOP Options radios
[x] Header sort / Undo preserves Overlay formatting
[x] QAQC vs Overlay order
[x] Password paste while masked
[x] Export complete message


Optional smoke (earlier code, not re-checked)
--------------------------------------------------------------------------------
[ ] Large dataset warning removed (>10k Yes/No gone)
[ ] Header sort not stacked (sectionClicked once)
