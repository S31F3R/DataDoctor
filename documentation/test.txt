TEST FILE — tests to run
============================================================
Grok writes the steps. Mark [x] when it passes, or note a fail.
Workflow / publish how-to: documentation/status.grok (gitignored).
“see updates” = collapse [x], tend RESPONSE, do To Do CURRENT TASKS/BUGS.
Current Tests: untested / failed / retest. Passed [x] items collapse to Confirmed.
Confirmed: tests passed since the last published X.Y.Z. A published
publishRelease.py folds any titles missed in the change list, then clears this.

Current Tests
--------------------------------------------------------------------------------
[ ] Mac package script
   Steps:
     a. From project root: python scripts/packageMac.py --skip-venv
     b. dist/DataDoctor-macOS-YYYYMMDD.zip contains Data Doctor.command + pythonFiles/
        + updates/ + applyUpdate.sh + pythonFiles/certs/
     c. On a Mac: unzip, double-click Data Doctor.command (needs Python 3.13 / venv).
     d. Optional on Mac only: python scripts/packageMac.py --app → .app via PyInstaller.

[ ] Check Beta on published — no dialog
   Steps:
     a. Install is a published X.Y.Z (not -rc / -beta), Beta updates currently unchecked.
     b. Options → check Beta updates → Save.
     c. No popup if there is no newer rc/beta (not “You're on the latest version”).
     d. If a newer rc/beta exists, the usual Update available dialog is OK.

[ ] New-setup Add Data Type defaults
   RESPONSE: Aquarius was checked, it's supposed to be unchecked
   Steps:
     a. Quit Data Doctor. Delete only the live user.config:
        Linux: ~/.config/Data Doctor/user.config
        Windows: %LOCALAPPDATA%\Data Doctor\user.config
        macOS: ~/Library/Application Support/Data Doctor/user.config
        (Not ~/.config/user.config or ~/.config/USBR/Data Doctor/ — leftovers.)
     b. Launch. Options → Add Data Type to Labels: USBR and USGS checked,
        Aquarius unchecked (Aquarius tab).
     c. Existing saved config is left as the user last saved it.

[ ] Bunker merge threads + Merging status
   Steps:
     a. Run a dictionary merge (applyUpdate or updateBunker.py). After
        Backup: path, console shows Merging... before Merge complete.
     b. Merge still updates siteName/database, prompts commonName/datatype,
        fills blanks only, inserts packaged-only rows.

[ ] First-install bunker: no merge prompts
   Steps:
     a. Install with no live pythonFiles/core/bunker.db (or a copy that
        matches the packaged file). applyUpdate / updateBunker does not ask
        “Update Data Dictionary Common Names?” or Data Types.
     b. When a different live bunker.db already exists, the y/n prompts still
        appear.

[ ] GitHub update check blocked
   Steps:
     a. With GitHub unreachable (blocked/timeout): startup or update check
        shows “Unable to check GitHub for updates.” (not silent, not “already
        current”).
     b. When GitHub works and you are current: still no popup.

[ ] Windows packageWindows.py removes winStage
   Steps:
     a. On Windows, from project root: python scripts/packageWindows.py
     b. dist/DataDoctor-Windows-YYYYMMDD.zip (or --out path) exists.
     c. dist/winStage* is gone after “Done: …”.

Confirmed
--------------------------------------------------------------------------------
Packaged Quick Looks adopt then remove
About Report issue → Form
About Feature request Form
Graph no axis titles; Y ticks match line
SQL dialogs parent to SQL window
SQL result column filters
Filter click-out cancels
Header Remove column
Multi-column header drag
Uncheck Beta on published — no dialog
Profile Data Dictionary
Data Dictionary auto-size on open

Deferred / known
--------------------------------------------------------------------------------
[!] AppImage applyUpdate path not wired the same as launcher packages yet
[!] Rebuild Data Doctor.exe on Windows to ship launcher VB first-run extract message
