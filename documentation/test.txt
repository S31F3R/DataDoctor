TEST FILE — currently being worked / needing retest
============================================================
Workflow:
  - To Do List.txt = backlog only
  - This file = in-progress / Seifer tests here
  - PASS → mark [x] here (stay off To Do)
  - FAIL → note here; Grok re-fixes


Need retest (this pass)
--------------------------------------------------------------------------------

1) Options: remask secrets on close
   Steps:
     a. Open Options → reveal AQ password, USGS key, and/or Oracle password (eye icons).
     b. Close via OK, Cancel, or window X.
     c. Re-open Options.
   EXPECT: all three secret fields are masked again; eye icons show Hidden state.
   Notes: hideEvent + showEvent call maskSensitiveFields().

2) SQL Query Builder does not freeze UI
   Steps: Enable SQL, run a non-trivial Oracle query (or even a slow one).
   EXPECT: main window stays responsive while query runs (can drag window, click elsewhere);
           Run button disabled until done; results fill sqlTable when finished.
           Wrong password still shows single login-failed style message.
   Notes: Oracle work runs on QThreadPool; table fill on main thread when done.

3) Wrong HDB/Oracle password (if not re-checked yet)
   Steps: Wrong Oracle password → HDB or SQL query.
   EXPECT: clear auth error; no multi-thread login spam; fixed by saving creds in Options.


Known / not a pure Python fix
--------------------------------------------------------------------------------
[!] Taskbar icon on Windows when launched via VB.NET → Python
    Linux OK. Python process icon is controlled by the host .exe / shortcut.
    Fix belongs on the VB launcher (or ship a PyInstaller/DataDoctor.exe with --icon).
    Left off active work unless you want a launcher-side change.


Confirmed (do not re-break)
--------------------------------------------------------------------------------
[x] Aquarius Label = Label only
[x] About secret π icon
[x] Header Max/Min show timestamps
[x] HDB r_base fallback coloring
[x] Export CSV uses active tab
[x] HDB headers = commonName - datatype
[x] Details max 16 rows + themed scrollbar
[x] Overlay/HDB no ghost 3rd column
[x] USGS meta no duplicate rows / USGS-only meta
[x] USGS headers use Site Name (comma split)
[x] Save Quick Look dialog wider
[x] Query window wider (USGS UID)
[x] Raw data deltas without scientific notation
[x] End timestamps exclusive end
[x] BOP/EOP Options radios
[x] Header sort / Undo preserves Overlay
[x] QAQC vs Overlay order
[x] Password paste while masked
[x] Export complete message


Optional smoke
--------------------------------------------------------------------------------
[ ] Large dataset warning removed (>10k Yes/No gone)
[ ] Header sort not stacked (sectionClicked once)
