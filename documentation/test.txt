TEST FILE — currently being worked / needing retest
============================================================
Workflow:
  - To Do List.txt = backlog only
  - This file = in-progress / Seifer tests here
  - PASS → mark [x] here (stay off To Do)
  - FAIL → note here; Grok re-fixes


Need retest (this pass)
--------------------------------------------------------------------------------

1) About secret π icon (restored clean glyph)
   Steps: Open About → bottom-right corner.
   EXPECT: clean white π icon (same as original Secret.png look), NOT CRT/TV static.
   Notes: pi.png is now the clean 48×48 glyph (copy of what worked as Secret.png).
          Full movie still stays at ui/icons/pi.jpg for reference only — not used as button icon.

2) Header Max/Min show timestamps
   Steps: After a data query, right-click a column header → Show details (or Max/Min path).
   EXPECT: Max and Min rows include the timestamp, e.g. "12.34 @ 07/01/26 14:00:00".
           Mean can stay value-only.

3) HDB r_base fallback coloring
   Steps: Internal USBR query where some timesteps have r_base value but no interval value
          (cell shows a number filled from base).
   EXPECT: those cells = light blue background (same family as missing) + black text.
           Pure missing (no base either) still blue empty. Normal interval values keep QAQC colors.

4) Aquarius Label first in meta details
   Steps: Internal Aquarius query → cell Show details.
   EXPECT: first row is "Aquarius Label" with Label / LocationIdentifier.
           Parameter and the rest follow.


Confirmed (do not re-break)
--------------------------------------------------------------------------------
[x] Export CSV uses active tab
[x] HDB headers = commonName - datatype
[x] Details max 16 rows + themed scrollbar
[x] Overlay/HDB no ghost 3rd column
[x] USGS meta no duplicate rows
[x] USGS meta on USGS-only / non-overlay
[x] USGS headers use Site Name (comma split)
[x] Save Quick Look dialog wider (~+60px)
[x] Query window wider (USGS UID)
[x] Raw data deltas without scientific notation
[x] End timestamps exclusive end
[x] BOP/EOP Options radios
[x] Header sort / Undo preserves Overlay formatting
[x] QAQC vs Overlay order
[x] Password paste while masked
[x] Export complete message


Optional smoke
--------------------------------------------------------------------------------
[ ] Large dataset warning removed (>10k Yes/No gone)
[ ] Header sort not stacked (sectionClicked once)
