TEST FILE — currently being worked / needing retest
============================================================
Workflow:
  - To Do List.txt = backlog only
  - This file = in-progress / Seifer tests here
  - PASS → mark [x] here (stay off To Do); Grok may clean confirmed [x] items
  - FAIL → note here; Grok re-fixes
  - Grok may commit any non-documentation/ paths; documentation/ commits stay with Seifer
    (README.md and other non-docs paths are fair game)

Current Tests
--------------------------------------------------------------------------------

[ ] Update dialogs: restart, not applyUpdate.cmd
   Steps:
     a. Windows launcher + Beta updates. When an update is offered, text says
        restart Data Doctor (no applyUpdate.cmd).
     b. After Download, "Restart Data Doctor to apply the update."
     c. Progress dialog: bar has even left/right margins (not shoved left).

[ ] applyUpdate keeps a single bunker.db.bak
   Steps:
     a. Run applyUpdate twice (or merge bunker twice).
     b. Project Files/core/ has at most one bunker.db.bak (no .bak-timestamp pile).

[ ] Header multi-select — RETEST Ctrl add (step D)
   Steps:
     a. Click column header A → whole column selected + header highlight.
     b. Shift+click header D → columns A–D all selected. Origin stays A.
     c. Ctrl+click header B (when B is selected) → B deselects; others stay.
     d. Ctrl+click an unselected header (e.g. F) → F is *added*; A–D+F stay.
        Must not collapse to only F. Must not make F the Shift origin.
     e. After that Ctrl+click, Shift+click another header still extends from
        the last *plain* click (A), not from F or D.
     f. Plain-click a new column, then Shift+click — new range from that click.
     g. Cell multi-select still works; header Graph uses all selected columns.
     h. Dragging a header edge still resizes the column.
   Notes: A/B OK. Shift origin no longer stolen by Ctrl.
     RESPONSE: Ctrl still selects then rapidly unselects (Linux + Windows).
     Clicks hit the header viewport; filter on the header never saw them, so
     Qt selectColumn ran after us and toggled the column off.
   Fix: filter on header.viewport(); eat press+release so sectionClicked
     / selectColumn do not run.

[ ] Crash / About Report on GitHub (option C, no stored login)
   Steps:
     a. Trigger or simulate an uncaught error — dialog has Report on GitHub,
        Copy log, Open log folder. Report opens a pre-filled new-issue page
        (needs a GitHub account; GitHub's own login).
     b. About → Report issue link opens /issues/new/choose (bug / crash forms).
     c. No GitHub token / password fields in Options.

[ ] HDB password change UI only shows useful outcomes
   Steps:
     a. Change Oracle password in Options → USBR (user that exists on some HDBs).
     b. Popup lists successes; wrong-password DBs get per-DB prompt; locked accounts listed.
     c. Missing users and TNS/network noise stay out of the popup (still in app.log).
     d. On per-DB wrong-password prompt: Cancel is labeled Skip — skips that DB and
        continues remaining HDBs (listed under "Skipped" in summary, not as errors).
   Notes: status 'locked' shown; 'error'/'missing' silent in UI.
   Fix: Cancel → Skip continues queue; skipped DBs separate from errors.

[ ] HDB database-link failure → direct connect fallback
   Steps:
     a. Internal multi-DB query where primary uses @link to another HDB.
     b. If link fails (ORA-02019 etc.), retry by connecting to the target DSN directly.
     c. Query still returns data when direct path works.
   Notes: USBR.sqlRead detects link errors and re-runs without link.

[ ] Mac package script
   Steps:
     a. From project root: python scripts/packageMac.py --skip-venv
     b. dist/DataDoctor-macOS-YYYYMMDD.zip contains Data Doctor.command + Project Files/
        + Update/ + applyUpdate.sh
     c. On a Mac: unzip, double-click Data Doctor.command (needs Python 3.13 / venv).
     d. Optional on Mac only: python scripts/packageMac.py --app → .app via PyInstaller.

[ ] Update check after a real release (when you publish one)
   Steps:
     a. Bump Version.VERSION lower than a published tag OR publish a higher tag with assets:
        - Launcher: DataDoctor-Python.zip (or *python*.zip)
        - AppImage: DataDoctor-x86_64.AppImage (or zip containing it)
     b. Stable channel ignores Pre-release tags; Beta channel includes them (-rc / -beta).
     c. Prompt → Download → file lands in Update/ next to install / AppImage.
     d. Launcher: close + applyUpdate. AppImage: Quit and apply (or applyAppImageUpdate.sh).


Confirmed
--------------------------------------------------------------------------------
[x] About: no scrollbars; ratio lock; maximize still works
[x] Child windows minimize to the taskbar (About + Dictionary + Query)
[x] publishRelease.py dry-run + build-only
[x] publishRelease re-upload same tag (no 422)
[x] publishRelease --upload without gh CLI
[x] GitHub README + wiki first pass
[x] Update check silent when nothing newer (incl. no releases)
[x] applyUpdate writes DataDoctor.pyw (Windows / existing .pyw)
[x] Graph hover shows table values (raw when raw is on); axis stays clean
[x] Query finish selects Data Query tab
[x] Overlay / delta use the display limiter (raw mode)
[x] USGS / Aquarius Data Dictionary second-line source tag
[x] Multi Y-axis graph (distinct value bands + hide axis with legend)
[x] Graph Y-scale fits data in current X view (outlier off-screen)
[x] Graph legend toggle keeps current X zoom
[x] Header Graph uses full multi-column selection (not just click col)
[x] Graph markers dense when zoomed (1-minute data)
[x] Beta updates checkbox (UI-owned, code wire-up only)
[x] Graph legend ☑/☐ toggle works while Zoom is default-on
[x] Windows package: no root updateBunker.cmd; applyUpdate merges bunker
[x] Graph always shows data-point markers (dots)
[x] Graph Y-axis keeps absolute values when zoomed (no offset scale)
[x] Fresh graph pan at full range has no visual "adjust"/resize
[x] Refresh bumps end time to now (unless historical > 2h)
[x] Python zip + applyUpdate (first pass)
[x] USGS-NWIS intervals: no HOUR; default INSTANT:15
[x] Options password fields: no layout WARN spam
[x] Graph opens with Zoom tool active
[x] Graph save remembers last folder
[x] Context menu Graph + selection window
[x] Aquarius Server Link placeholder
[x] Goat stress-relief button plays scream
[x] SQL Query Builder opens with zero tabs open
[x] Graph middle-mouse pan + range clamp
[x] Windows first-load icon (retest / improve-only)
[x] Graph hover tooltip stable at top edge (no frame jump)
[x] Graph arrow keys pan the view
[x] Graph series legend click-to-toggle (☑/☐) + rescale
[x] Column header highlight — all selected columns
[x] Graph overlay legends use header first-line labels
[x] Quick Look missing delta/overlay meta → checkboxes unchecked
[x] Prev Day / Prev Week times refresh on open, load quick look, and Query
[x] Windows package script layout (WinZip flat view was a false alarm)
[x] commonName-datatype header (no spaces around dash)
[x] Log Viewer button toggles tab (open / close)
[x] Graph: system dark/light colors + blank title + hover tooltip + table timestamps
[x] Graph toolbar button tooltips
[x] Query list: Insert Above / Insert Below
[x] Clear Query List unchecks Deltas and Overlay
[x] btnSQL toggles SQL Query Builder tab
[x] Query list double-click edit allows interval (and database) change
[x] Data dictionary headers use "commonName - datatype" for all DBs (pre-space-strip)
[x] SQL + Graph button tooltips and hover/pressed icons
[x] Graph button plots Data Query table into Graph tab
[x] Detach / Attach for Graph and Log Viewer tabs only
[x] USGS dataDictionary lookup (bunker PVLC — precisionOverride DEC(2))
[x] Data Dictionary Ctrl+S / Save button (combobox commit + executemany)
[x] Data Dictionary combo column widths (sizeFromContents + scrollbar)
[x] Cell-level Copy (Details + Data Dictionary; SelectItems)
[x] AppImage tool path under scripts/appimagetool/
[x] Data Dictionary database column is a dropdown
[x] Options tab focus + tab order
[x] App icons layout
[x] Linux AppImage packager script (+ oracledb getpass fix)
[x] MRID internal query (values only; no metadata)
[x] Windows package + bunker merge scripts
[x] Clipboard copy/paste (Excel-like) — main data table
[x] Delta columns skip normal QAQC coloring
[x] UCHDB2 schema UCHDBA via config
[x] Aquarius certs: no random certs/ folders + convert cleanup (.cer/.pem only; no .pfx)
[x] Timestamps centered in vertical header column
[x] Smarter query dates for DAY+ intervals
[x] USGS DataID: optional parameter + Site-Parameter lookup (resolveUsgsDataId / picker)
[x] Interval timestamps: DAY / MONTH / YEAR / WATER YEAR display formats
[x] Details window resizes down when switching tabs
[x] Packaged Oracle preferred; system fallback
[x] winMain tooltips (simplified labels)
[x] Header size no longer changes when clicking a cell
[x] Overlay secondary-only → auto edit for upload
[x] USGS public query: Show details on right-click
[x] valuePrecision.json lives under core/
[x] valuePrecision + precisionOverride (bunker.db + formatting)
[x] Logging: full Python tracebacks + Qt in app.log / Log Viewer
[x] USGS interval combo: no intervals past DAY when USGS-NWIS selected
[x] Fonts/layout: Noto default ↔ retro toggle (Linux then Windows)
[x] Log Viewer tab
[x] Keep connection reuse (C); do NOT re-add batch SDIDs (A)
[x] Option C connection reuse
[x] V1 large table build + overlay
[x] PATH Instant Client env limit
[x] Prior bug-smash cluster
[x] HDB MODIFY_R_BASE / DELETE_R_BASE write path
[x] Delete key multi-clear
[x] Delta never shows signed zero (-0.00)
[x] Aquarius-only query: Show details on right-click
[x] Details window title not clipped
[x] Retro mode: session frozen until full restart
[x] Header: single-click highlight; double-click sort
[x] Windows taskbar icon (AppUserModelID + window icon)
[x] User config + quickLooks load from profile again
[x] Options first open after startup is snappy

Deferred / known
--------------------------------------------------------------------------------
[!] USGS monthly/yearly (OGC has no products)
[!] Aquarius daily 2300 TZ
[!] Features still backlog: advanced plot tool / quick-look plots, git/readme,
    dataDictionary non-destructive bulk update, expanded Options tree
[!] Windows first-load icon — improve-only
[!] AppImage applyUpdate path not wired the same as launcher packages yet
