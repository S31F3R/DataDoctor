TEST FILE — currently being worked / needing retest
============================================================
Workflow:
  - To Do List.txt = backlog only
  - This file = in-progress / Seifer tests here
  - PASS → mark [x] here (stay off To Do); Grok may clean confirmed [x] items
  - FAIL → note here; Grok re-fixes


Need retest (longer tests — Seifer tomorrow)
--------------------------------------------------------------------------------

[ ] HDB password change UI only shows useful outcomes
   Steps:
     a. Change Oracle password in Options → USBR (user that exists on some HDBs).
     b. Popup lists successes; wrong-password DBs get per-DB prompt; locked accounts listed.
     c. Missing users and TNS/network noise stay out of the popup (still in app.log).
   Notes: status 'locked' shown; 'error'/'missing' silent in UI.

[ ] UCHDB2 schema UCHDBA via config
   Steps:
     a. Internal query against USBR-UCHDB2 (data + metadata maps).
     b. Comboboxes/labels show USBR-UCHDB2 only (no |UCHDBA).
     c. Logs/DEBUG schema should use UCHDBA for UCHDB2.
   Notes: Config.hdbOracleDatabases 'USBR-UCHDB2|UCHDBA'; Utils.hdbSchemaForDatabase.

[ ] USGS DataID: optional parameter + Site-Parameter lookup
   Steps:
     a. Add Query with Site-time_series_id only (no -00065) → query succeeds.
     b. Site-time_series_id-00065 still works.
     c. Site-00065 (param only): if one series, auto-resolves; if many, picker dialog.
     d. Cancel picker → not added to list.
   Notes: USGS.classifyUid / resolveUsgsDataId; OGC filter is time_series_id only.

[ ] Packaged Oracle preferred; system fallback
   Steps:
     a. With oracle/client present → uses packaged Instant Client (DEBUG log).
     b. Without packaged client → system Instant Client or thin mode (WARN/INFO log).
     c. sqlnet.ora: if TNS_ADMIN unset, packaged oracle/network/admin is used.
     d. Existing TNS_ADMIN env still wins for tnsnames (unchanged).

[ ] Interval timestamps: DAY / MONTH / YEAR / WATER YEAR display formats
   Steps:
     a. DAY query → vertical header like 03/15/24 (no time).
     b. MONTH query → header like 03/24 (mm/yy).
     c. YEAR and WATER YEAR → header like 2024 (yyyy only).
     d. HOUR / INSTANT unchanged (mm/dd/yy HH:MM:00).
     e. Sort by timestamp and cell details still work on coarser headers.
   Notes: Query.formatTimestamp / buildTimestamps / gapCheck interval-aware.


Need retest (fixes after RESPONSE)
--------------------------------------------------------------------------------

[ ] Retro mode restart on Windows
   Steps:
     a. Options → toggle Retro Mode → OK to restart.
     b. App should relaunch with the new mode (Windows).
     c. Cancel on the prompt reverts the checkbox and config.
   Notes: Uses temp .cmd delay + absolute paths (prior QProcess/execl failed on Win).
   Prior RESPONSE: Still only worked on Linux.

[ ] Details window resizes down when switching tabs
   Steps:
     a. Overlay cell → Show details.
     b. Open Aquarius Details first (wide 4-col), then USBR-*-Details (2-col).
     c. Window should shrink to fit the narrower tab on first switch (not stay stuck wide).
     d. Reverse order also fine.
   Notes: Measure with stretchLastSection off; setFixedSize then unlock; deferred initial size.
   Prior RESPONSE: Stayed super wide after wide tab first; only first couple clicks.

[ ] winMain tooltips (simplified labels)
   Steps:
     a. Public Query tip: "Public Queries"
     b. Internal Query tip: "Internal Queries"
     c. Options tip: "Options" (no parenthetical list)
     d. Upload still mentions HDB MODIFY/DELETE; other tips unchanged and good.


Need retest (new features this pass)
--------------------------------------------------------------------------------

[ ] Double-click Query List item → edit dataID
   Steps:
     a. Add several queries to the list.
     b. Double-click one → only dataID appears in the text box; interval/DB stay as that row.
     c. Change dataID, hit Add Query → that list row updates in place (not a new row).
     d. Add Query with empty edit-index still appends a new row as before.

[ ] SQL snippets reorder (up/down) persists
   Steps:
     a. SQL Query Builder: select a snippet, move up/down.
     b. Order remains after close/reopen app.
   Notes: order stored in user.config sqlSnippetOrder.

[ ] Aquarius certs: .cer / .pfx accepted
   Steps:
     a. Place aquarius.cer or aquarius.pfx (or any .cer/.pfx) under certs/.
     b. On next Aquarius query, code converts to certs/aquarius.pem and uses it.
     c. Optional: leftover conversion temp cleaned; pem remains for requests.
   Notes: Logic.ensureAquariusPem / Aquarius.apiRead.

[ ] HDB database-link failure → direct connect fallback
   Steps:
     a. Internal multi-DB query where primary uses @link to another HDB.
     b. If link fails (ORA-02019 etc.), retry by connecting to the target DSN directly.
     c. Query still returns data when direct path works.
   Notes: USBR.sqlRead detects link errors and re-runs without link.

[ ] Windows package + bunker merge scripts
   Steps:
     a. scripts/packageWindows.py builds zip with launcher as root, Project Files layout.
     b. launcher/updateBunker.py merges packaged bunker.db into user bunker without wiping
        user fields except dataType/siteName/valuePrecision/database when dataID+siteID match.


Confirmed
--------------------------------------------------------------------------------
[x] Header size no longer changes when clicking a cell
[x] Overlay secondary-only → auto edit for upload
[x] USGS public query: Show details on right-click
[x] valuePrecision.json lives under core/
[x] valuePrecision + precisionOverride (bunker.db + formatting)
[x] Logging: full Python tracebacks + Qt in app.log / Log Viewer
[x] USGS interval combo: no intervals past DAY when USGS-NWIS selected
[x] Fonts/layout: Noto default ↔ retro toggle (Linux then Windows)
[x] Log Viewer tab
[x] Keep connection reuse (C); do NOT re-add batch SDIDs (A)
[x] Option C connection reuse
[x] V1 large table build + overlay
[x] PATH Instant Client env limit
[x] Prior bug-smash cluster
[x] HDB MODIFY_R_BASE / DELETE_R_BASE write path
[x] Delete key multi-clear


Deferred / known
--------------------------------------------------------------------------------
[!] Windows taskbar via VB launcher (not fixable in Python alone)
[!] USGS monthly/yearly (OGC has no products)
[!] Aquarius daily 2300 TZ
[!] Features still backlog: plots, goat, git/readme, dataDictionary non-destructive bulk update
)
