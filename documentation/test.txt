TEST FILE — currently being worked / needing retest
============================================================
Workflow:
  - To Do List.txt = backlog only
  - This file = in-progress / Seifer tests here
  - PASS → mark [x] here (stay off To Do); Grok may clean confirmed [x] items
  - FAIL → note here; Grok re-fixes

Current Tests
--------------------------------------------------------------------------------

[ ] HDB password change UI only shows useful outcomes
   Steps:
     a. Change Oracle password in Options → USBR (user that exists on some HDBs).
     b. Popup lists successes; wrong-password DBs get per-DB prompt; locked accounts listed.
     c. Missing users and TNS/network noise stay out of the popup (still in app.log).
   Notes: status 'locked' shown; 'error'/'missing' silent in UI.

[ ] HDB database-link failure → direct connect fallback
   Steps:
     a. Internal multi-DB query where primary uses @link to another HDB.
     b. If link fails (ORA-02019 etc.), retry by connecting to the target DSN directly.
     c. Query still returns data when direct path works.
   Notes: USBR.sqlRead detects link errors and re-runs without link.

[ ] Windows package + bunker merge scripts
   Steps:
     a. python scripts/packageWindows.py
        - Zip has README.txt + UPDATE.txt (generated each run).
        - Project Files/temp/bunker.db present (merge source).
        - Project Files/core/bunker.db present (live / fresh install).
        - Project Files/scripts/updateBunker.py present.
        - updateBunker.cmd at zip root calls that script.
     b. Fresh unzip: app runs (Python 3.13 / provided installer notes in README).
     c. Simulate update: keep a user core/bunker.db with local edits; drop new
        package files; run updateBunker.cmd.
        - Merge updates only dataType/siteName/valuePrecision/database on match.
        - temp/ folder removed after successful merge.
   Notes: RESPONSE handled — readme per package; bunker in temp; script under
     Project Files/scripts; cmd fixed.

[ ] Retro mode restart on Windows
   Steps:
     a. Options → toggle Retro Mode → OK/save.
     b. Windows: dialog says setting was saved; close and reopen for it to apply
        (no auto-restart attempt, no cmd/ping error).
     c. After manual restart, retro mode matches the saved setting.
     d. Linux may still offer auto-restart (optional path).
   Notes: RESPONSE — auto-restart abandoned on Windows; manual restart message only.

[ ] Header: single-click highlight column; double-click sort
   Steps:
     a. Internal query with several columns.
     b. Single-click a column header → entire column highlighted (no sort).
     c. Double-click same header → table sorts by that column.
     d. Double-click again → sort toggles asc/desc.
     e. btnUndo still restores timestamp order.
   Notes: sectionClicked → selectEntireColumn; sectionDoubleClicked → customSortTable.

[ ] Clipboard copy/paste (Excel-like)
   Steps:
     a. Internal query. Select a multi-cell range (e.g. 5 rows × 1 column).
     b. Ctrl+C or right-click → Copy. Paste into a spreadsheet → grid intact.
     c. Select destination top-left cell (or range) in another column; Ctrl+V or
        right-click → Paste. Values fill down/across; cells turn magenta (edits).
     d. Public query: Copy works; Paste is not offered in the context menu and
        Ctrl+V does not write cells.
     e. Delta columns stay locked (paste skips them).
   Notes: Upload.copySelectionToClipboard / pasteClipboardToSelection; ExtendedSelection.

[ ] Delta columns skip normal QAQC coloring
   Steps:
     a. Internal query with Delta checked and QAQC on.
     b. Delta column keeps +/− orange/blue text colors only — no yellow/green/red
        QAQC backgrounds on delta cells.
     c. Normal series columns still get QAQC as before.
   Notes: Query.qaqc skips meta type == 'delta'.


Confirmed
--------------------------------------------------------------------------------
[x] UCHDB2 schema UCHDBA via config
[x] Aquarius certs: no random certs/ folders + convert cleanup (.cer/.pem only; no .pfx)
[x] Timestamps centered in vertical header column
[x] Smarter query dates for DAY+ intervals
[x] USGS DataID: optional parameter + Site-Parameter lookup (resolveUsgsDataId / picker)
[x] Interval timestamps: DAY / MONTH / YEAR / WATER YEAR display formats
[x] Details window resizes down when switching tabs
[x] Packaged Oracle preferred; system fallback
[x] winMain tooltips (simplified labels)
[x] Header size no longer changes when clicking a cell
[x] Overlay secondary-only → auto edit for upload
[x] USGS public query: Show details on right-click
[x] valuePrecision.json lives under core/
[x] valuePrecision + precisionOverride (bunker.db + formatting)
[x] Logging: full Python tracebacks + Qt in app.log / Log Viewer
[x] USGS interval combo: no intervals past DAY when USGS-NWIS selected
[x] Fonts/layout: Noto default ↔ retro toggle (Linux then Windows)
[x] Log Viewer tab
[x] Keep connection reuse (C); do NOT re-add batch SDIDs (A)
[x] Option C connection reuse
[x] V1 large table build + overlay
[x] PATH Instant Client env limit
[x] Prior bug-smash cluster
[x] HDB MODIFY_R_BASE / DELETE_R_BASE write path
[x] Delete key multi-clear


Deferred / known
--------------------------------------------------------------------------------
[!] Windows taskbar icon (python/.pyw and VB launcher) — platform/AppUserModelID
[!] USGS monthly/yearly (OGC has no products)
[!] Aquarius daily 2300 TZ
[!] Features still backlog: plots, goat, git/readme, dataDictionary non-destructive bulk update,
    SQL builder as button, expanded Options tree, Update/ folder unpackers, Linux/Mac package scripts
