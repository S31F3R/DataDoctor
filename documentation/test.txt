TEST FILE — currently being worked / needing retest
============================================================
Workflow:
  - To Do List.txt = backlog only
  - This file = in-progress / Seifer tests here
  - PASS → mark [x] here (stay off To Do); Grok may clean confirmed [x] items
  - FAIL → note here; Grok re-fixes

Current Tests
--------------------------------------------------------------------------------

[x] USGS dataDictionary lookup (bunker PVLC — precision is the proof)
   Concrete bunker row:
     dataID            = f02e05bac5ca472dbce494a3f54ef451  (bare hex only)
     siteID            = 09429100
     database          = USGS-NWIS
     commonName        = PVLC (USGS)
     valuePrecision    = Discharge   → Aquarius default DEC(3) if override ignored
     precisionOverride = DEC(2)      → MUST win
   Query forms (multi-part — Aquarius is bare UID and already works):
     09429100-f02e05bac5ca472dbce494a3f54ef451-60
     09429100-f02e05bac5ca472dbce494a3f54ef451
   Steps:
     a. Confirm dict row as above (override DEC(2), not blank).
     b. Query public/internal with site-tsid or site-tsid-param.
     c. VALUES must show 2 decimal places (DEC(2)), NOT 3 (Discharge/DEC(3)).
        That is the definitive proof the dict row + override applied.
     d. Header shows "PVLC (USGS)" (commonName), like Aquarius shows its label.
     e. Show details: OGC tags, not "legacy method IDs".
   Notes: Grok verified offline — buildTable seriesRules → DEC(2), cells e.g.
     1234.567 → 1234.57. Discharge alone would be 1234.567; total miss → DEC(2)
     default (also 2dp) so override was the real differentiator vs Discharge.
     Fix: seriesRules always resolve full query DataID→bare tsid + override.

[x] Data Dictionary Ctrl+S / Save button (reevaluated)
   Steps:
     a. Open Data Dictionary (large table OK — ~30k rows).
     b. Edit a normal cell → Ctrl+S → "Saved" dialog promptly (not hung).
     c. Double-click database or valuePrecision combobox, pick a value → Ctrl+S
        while editor still open (or after pick) → saves and dialog appears.
     d. Click Save toolbar button after a small edit → same "Saved" dialog.
     e. Re-open Data Dictionary → edits persisted.
   Notes: Commit open combobox before read; executemany insert; no full-table
     resizeColumnToContents after save; re-entrancy guard + wait cursor.
     Prior fail: save "never finished" after Ctrl+S + database combobox.

[x] Data Dictionary combo column widths
   Steps:
     a. Open Data Dictionary.
     b. database column wide enough for longest DB name (e.g. USBR-YAOHDB)
        and header — not clipped in the closed cell or dropdown.
     c. valuePrecision: open the dropdown; longest labels (e.g.
        "Attenuation turbidity  [SIG(3,1)]" / "Turbidity, FormBksctr  [SIG(3,1)]")
        fully visible including the vertical scrollbar on the popup list.
     d. After Save, those two columns stay sized (not collapsed).
   Notes: Prior RESPONSE — still clipped; scrollbar not in width math.
     Fix: size from QComboBox sizeFromContents + PM_ScrollBarExtent (≥20)
     + fudge so popup list does not clip.

[x] Cell-level Copy (Details + Data Dictionary)
   Steps:
     a. Query with metadata → right-click cell → Show details.
     b. Click a single cell (only that cell highlights, not whole row).
     c. Ctrl+C or right-click → Copy → paste is just that cell's text.
     d. Drag/Ctrl multi-select several cells → Copy → TSV grid in notepad.
     e. Right-click a cell outside the multi-selection → that cell alone is
        selected for Copy.
     f. Data Dictionary: same cell / multi-cell Copy via Ctrl+C and context menu.
   Notes: SelectItems (was SelectRows). Main data table already SelectItems.

[x] AppImage tool path under scripts/
   Steps:
     a. tools live in scripts/appimagetool/appimagetool-<arch>.appimage
        (not only dist/appimagetool/).
     b. python scripts/packageAppImage.py finds scripts/… tool automatically.
     c. Optional: --appimagetool still overrides; dist/appimagetool/ still works
        as fallback if scripts/ is empty.

[ ] HDB password change UI only shows useful outcomes
   Steps:
     a. Change Oracle password in Options → USBR (user that exists on some HDBs).
     b. Popup lists successes; wrong-password DBs get per-DB prompt; locked accounts listed.
     c. Missing users and TNS/network noise stay out of the popup (still in app.log).
     d. On per-DB wrong-password prompt: Cancel is labeled Skip — skips that DB and
        continues remaining HDBs (listed under "Skipped" in summary, not as errors).
   Notes: status 'locked' shown; 'error'/'missing' silent in UI.
   Fix: Cancel → Skip continues queue; skipped DBs separate from errors.

[ ] HDB database-link failure → direct connect fallback
   Steps:
     a. Internal multi-DB query where primary uses @link to another HDB.
     b. If link fails (ORA-02019 etc.), retry by connecting to the target DSN directly.
     c. Query still returns data when direct path works.
   Notes: USBR.sqlRead detects link errors and re-runs without link.


Confirmed
--------------------------------------------------------------------------------
[x] Data Dictionary database column is a dropdown
[x] Options tab focus + tab order
[x] App icons layout
[x] Linux AppImage packager script (+ oracledb getpass fix)
[x] MRID internal query (values only; no metadata)
[x] Windows package + bunker merge scripts
[x] Clipboard copy/paste (Excel-like) — main data table
[x] Delta columns skip normal QAQC coloring
[x] UCHDB2 schema UCHDBA via config
[x] Aquarius certs: no random certs/ folders + convert cleanup (.cer/.pem only; no .pfx)
[x] Timestamps centered in vertical header column
[x] Smarter query dates for DAY+ intervals
[x] USGS DataID: optional parameter + Site-Parameter lookup (resolveUsgsDataId / picker)
[x] Interval timestamps: DAY / MONTH / YEAR / WATER YEAR display formats
[x] Details window resizes down when switching tabs
[x] Packaged Oracle preferred; system fallback
[x] winMain tooltips (simplified labels)
[x] Header size no longer changes when clicking a cell
[x] Overlay secondary-only → auto edit for upload
[x] USGS public query: Show details on right-click
[x] valuePrecision.json lives under core/
[x] valuePrecision + precisionOverride (bunker.db + formatting)
[x] Logging: full Python tracebacks + Qt in app.log / Log Viewer
[x] USGS interval combo: no intervals past DAY when USGS-NWIS selected
[x] Fonts/layout: Noto default ↔ retro toggle (Linux then Windows)
[x] Log Viewer tab
[x] Keep connection reuse (C); do NOT re-add batch SDIDs (A)
[x] Option C connection reuse
[x] V1 large table build + overlay
[x] PATH Instant Client env limit
[x] Prior bug-smash cluster
[x] HDB MODIFY_R_BASE / DELETE_R_BASE write path
[x] Delete key multi-clear
[x] Delta never shows signed zero (-0.00)
[x] Aquarius-only query: Show details on right-click
[x] Details window title not clipped
[x] Retro mode: session frozen until full restart
[x] Header: single-click highlight; double-click sort
[x] Windows taskbar icon (AppUserModelID + window icon)
[x] User config + quickLooks load from profile again
[x] Options first open after startup is snappy

Deferred / known
--------------------------------------------------------------------------------
[!] USGS monthly/yearly (OGC has no products)
[!] Aquarius daily 2300 TZ
[!] Features still backlog: plots, goat, git/readme, dataDictionary non-destructive bulk update,
    SQL builder as button, expanded Options tree, Update/ folder unpackers, Mac package script
