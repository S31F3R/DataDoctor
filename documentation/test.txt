TEST FILE — tests to run
============================================================
Grok writes the steps. Mark [x] when it passes, or note a fail.
Workflow / publish how-to: documentation/status.grok (gitignored).
Current Tests: untested / failed / retest. Passed [x] items collapse to Confirmed.
Confirmed: tests passed since the last published X.Y.Z. A published
publishRelease.py folds any titles missed in the change list, then clears this.

Current Tests
--------------------------------------------------------------------------------
[ ] Mac package script
   Steps:
     a. From project root: python scripts/packageMac.py --skip-venv
     b. dist/DataDoctor-macOS-YYYYMMDD.zip contains Data Doctor.command + pythonFiles/
        + updates/ + applyUpdate.sh + pythonFiles/certs/
     c. On a Mac: unzip, double-click Data Doctor.command (needs Python 3.13 / venv).
     d. Optional on Mac only: python scripts/packageMac.py --app → .app via PyInstaller.

[ ] Windows update Restart + launcher hop
   Steps:
     a. Accept an in-app download. Dialog should say “Hit Restart to update…” with Restart / Later.
     b. Restart closes Data Doctor and runs applyUpdate.cmd (app comes back).
     c. Later leaves the zip in updates\; next Data Doctor.exe apply still works.
     d. 3.0.x / no python-embed: after a Python zip apply, Windows zip should chain
        (or be the asset downloaded in the first place). No second manual applyUpdate.

[ ] Overlay swap / update from secondary / primary format
   Steps:
     a. Overlay query, header Swap Primary/Secondary — values, query list, full header
        (label AND dataID line), delta, and overlay colors all flip. Swap again restores.
        Column width resizes to the new header.
        RESPONSE: Swap changed only the first header line; deltas went to 0; colors
        vanished on the second swap. Recalc now uses swapped natives + new primary rule
        (do not negate then formatDeltaValue(..., None)).
     b. Differing overlay cell → Update from secondary (single and multi-select).
        Ctrl+Z restores the old primary (and colors); Ctrl+Y re-applies.
        RESPONSE: Update worked; Ctrl+Z/Y did not (now one undo cmd + overlay UserRole).
     c. USGS/Aquarius primary: no Update from secondary (or it does nothing).
     d. Raw Data off: USBR 642.272 vs Aquarius 642.2724 at primary DEC(3) is NOT red;
        delta 0; Overlay Details shows both at primary precision; USBR/Aquarius tabs
        keep their own rounding.

[ ] Retro spacing + live apply
   Steps:
     a. Retro ON (Silkscreen). Default UI spacing except:
        - Query info buttons except Data ID
        - Refresh / Undo / Upload on the data table
        - Query window ~4 characters wider (dataID, search, add query, query list —
          USGS-NWIS should not need a horizontal slider)
     b. Neon green tabs (text #323232 not black) + green scrollbars. Font is Silkscreen.
     c. Retro + Light: query scrollbar TRACK is light; paddle stays green.
     d. Options → Retro ON/OFF applies immediately (no restart), like Light/Dark.
     e. Graph already open: Light/Dark/Retro restyles series + retro toolbar green.
     f. winAbout: no extra blank line between AUTHOR rows at retroFontSizeAdjust=4.

[ ] Formulas + custom columns
   Steps:
     a. Type =RO then Tab → ROUND(  Tooltip shows ROUND(n, digits).
     b. = then click D10: D10 outlined, token D10 in the editor matches that color.
     c. Esc leaves the cell unchanged.
     d. Formula on a queried cell clears QAQC/overlay paint; Ctrl+Z puts it back.
     e. Public query: insert a column, type a formula, fill down — no magenta data-edit flag.
     f. Header Show details is not blank (query info + max/min/mean).
     g. Rename header → Query or Refresh: prompt BEFORE the query. Don't save →
        Refresh must not keep the unsaved label.

[ ] SQL query/result dotted splitter
   Steps:
     a. SQL tab: query/result handle matches the snippet-pane dotted grabber.
     b. Retro: those dots are green on both splitters.

Confirmed
--------------------------------------------------------------------------------
Windows silent launch (VM + laptop)
Query list scroll / no double query
Graph missing-data gaps
Windows first-run extract
Retro Silkscreen
Header click vs sort vs drag
Retro mode restart (no crash)
First-open Graph / Options / About
Table undo/redo
Insert / drag / rename columns
Double-click cell (no edit) colors
Light/Dark SQL theme

Deferred / known
--------------------------------------------------------------------------------
[!] AppImage applyUpdate path not wired the same as launcher packages yet
[!] Rebuild Data Doctor.exe on Windows to ship launcher VB first-run extract message
