TEST FILE — currently being worked / needing retest
============================================================
Workflow:
  - To Do List.txt = backlog only
  - This file = in-progress / Seifer tests here
  - PASS → mark [x] here (stay off To Do); Grok may clean confirmed [x] items
  - FAIL → note here; Grok re-fixes

Current Tests
--------------------------------------------------------------------------------

[ ] HDB password change UI only shows useful outcomes
   Steps:
     a. Change Oracle password in Options → USBR (user that exists on some HDBs).
     b. Popup lists successes; wrong-password DBs get per-DB prompt; locked accounts listed.
     c. Missing users and TNS/network noise stay out of the popup (still in app.log).
   Notes: status 'locked' shown; 'error'/'missing' silent in UI.

[ ] UCHDB2 schema UCHDBA via config
   Steps:
     a. Internal query against USBR-UCHDB2 (data + metadata maps).
     b. Comboboxes/labels show USBR-UCHDB2 only (no |UCHDBA).
     c. Logs/DEBUG schema should use UCHDBA for UCHDB2.
   Notes: Config.hdbOracleDatabases 'USBR-UCHDB2|UCHDBA'; Utils.hdbSchemaForDatabase.

[ ] HDB database-link failure → direct connect fallback
   Steps:
     a. Internal multi-DB query where primary uses @link to another HDB.
     b. If link fails (ORA-02019 etc.), retry by connecting to the target DSN directly.
     c. Query still returns data when direct path works.
   Notes: USBR.sqlRead detects link errors and re-runs without link.

[ ] Windows package + bunker merge scripts
   Steps:
     a. scripts/packageWindows.py builds zip with launcher as root, Project Files layout.
        RESPONSE: Works. Need a readme each package run (Python 3.13, python --version,
                run provided installer if missing).
     b. launcher/updateBunker.py merges packaged bunker.db into user bunker without wiping
        user fields except dataType/siteName/valuePrecision/database when dataID+siteID match.
        RESPONSE: Move updateBunker.py into Project Files/scripts (done). Add update readme.
                Put packaged bunker.db in Project Files/temp; updateBunker.cmd merges then
                removes temp folder. updateBunker.py already in the right place.

[ ] Retro mode restart on Windows
   Steps:
     a. Options → toggle Retro Mode → OK to restart.
     b. App should relaunch with the new mode (Windows).
     c. Cancel on the prompt reverts the checkbox and config.
   Notes: Prefer same python+script; bat logs to %TEMP%\\DataDoctorRestart.log;
     hidden detached cmd. If fail, open that log file for the real error.
   Prior RESPONSE: terminal popup with nonsensical error.
   RESPONSE: [error 2147942632 (0x800700e8) when launching `ping  -n 3 127.0.0.1 ']
            If this doesn't get fixed next round, change Windows popup to say they need
            to restart the program for changes to take effect.

[ ] Aquarius certs: no random certs/ folders + convert cleanup  **PRIORITY**
   Steps:
     a. Ensure project has an existing certs/ folder (create manually if needed — app will not).
     b. Place cert.cer (or any .cer/.pfx) under project certs/.
     c. Run an Aquarius query (DEBUG ON).
     d. app.log: found files includes the source; converted … → aquarius.pem; source removed.
     e. certs/aquarius.pem exists; cert.cer / .pfx is gone after success.
     f. No new empty certs/ folders outside the project (parent dir, cwd, ~/.config, etc.).
     g. If only a stale aquarius.pem remains and a new .cer fails to convert, app falls back
        to system trust then unverified (does not crash on old pem).
   Notes: Logic.ensureAquariusPem never makedirs certs/; writes pem next to source only;
     removes .cer/.crt/.pfx/.p12 after successful convert. Empty stray certs dirs cleaned
     on this pass (parent Projects/certs, ~/.config/certs, ~/.config/-c/certs).
   Prior RESPONSE: certs/cert.cer not converting; need remove source after convert;
     .pfx path; stale pem should not crash.
   RESPONSE (code): fixed makedirs bug; remove source; no stale-pem fallback when reconvert fails.

[ ] Timestamps centered in vertical header column
   Steps:
     a. Run any internal query so the main table has a timestamp rail.
     b. Timestamp text should be horizontally centered in the left header column.
     c. Still centered after overlay/delta rewrite and after sorting by a value column.
   Notes: buildTable + QueryUtils overlay path + updateTableAfterSort set AlignCenter.

[ ] Smarter query dates for DAY+ intervals
   Steps:
     a. Query with DAY interval; pick start/end that include non-zero times (e.g. 14:37).
     b. DEBUG log shows normalized range with 00:00 on both ends (time truncated).
     c. MONTH → start/end snapped to 1st of month 00:00; YEAR → Jan 1; WATER YEAR → Oct 1 of WY.
     d. HOUR / INSTANT unchanged (existing round-down only on minutes as before).
     e. Interval selection still from first list item / combo — do not change that logic.
   Notes: Query.roundDownToInterval + executeQuery applies to both start and end.


Confirmed
--------------------------------------------------------------------------------
[x] USGS DataID: optional parameter + Site-Parameter lookup (resolveUsgsDataId / picker)
[x] Interval timestamps: DAY / MONTH / YEAR / WATER YEAR display formats
[x] Details window resizes down when switching tabs
[x] Packaged Oracle preferred; system fallback
[x] winMain tooltips (simplified labels)
[x] Header size no longer changes when clicking a cell
[x] Overlay secondary-only → auto edit for upload
[x] USGS public query: Show details on right-click
[x] valuePrecision.json lives under core/
[x] valuePrecision + precisionOverride (bunker.db + formatting)
[x] Logging: full Python tracebacks + Qt in app.log / Log Viewer
[x] USGS interval combo: no intervals past DAY when USGS-NWIS selected
[x] Fonts/layout: Noto default ↔ retro toggle (Linux then Windows)
[x] Log Viewer tab
[x] Keep connection reuse (C); do NOT re-add batch SDIDs (A)
[x] Option C connection reuse
[x] V1 large table build + overlay
[x] PATH Instant Client env limit
[x] Prior bug-smash cluster
[x] HDB MODIFY_R_BASE / DELETE_R_BASE write path
[x] Delete key multi-clear


Deferred / known
--------------------------------------------------------------------------------
[!] Windows taskbar via VB launcher (not fixable in Python alone)
[!] USGS monthly/yearly (OGC has no products)
[!] Aquarius daily 2300 TZ
[!] Features still backlog: plots, goat, git/readme, dataDictionary non-destructive bulk update
