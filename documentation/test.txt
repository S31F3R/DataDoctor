TEST FILE — tests to run
============================================================
Grok writes the steps. Mark [x] when it passes, or note a fail.
Workflow / publish how-to: documentation/status.grok (gitignored).
Current Tests: untested / failed / retest. Passed [x] items collapse to Confirmed.
Confirmed: tests passed since the last published X.Y.Z. A published
publishRelease.py folds any titles missed in the change list, then clears this.

Current Tests
--------------------------------------------------------------------------------
[x] Windows silent launch (VM + laptop)
   Steps:
     a. Work laptop: existing install still opens; About (Info) still plays music.
     b. VM that previously died with no window: unzip a build with this app.pyw
        (Python zip via applyUpdate.cmd is enough; new Data Doctor.exe is optional).
        Double-click Data Doctor.exe. Main window should appear.
     c. If it still does not: open %LOCALAPPDATA%\Data Doctor\logs\app.log.
        Expect applyUpdate: / startup: / launcher: lines. Last startup: line is
        how far it got. fault.log if python aborted natively.
     d. Optional: rebuild launcher on Windows
        (see launcher/src/README.txt). Immediate pythonw death should then
        MessageBox instead of a silent return.
     e. Info button on the VM: About should open. Missing audio is OK (no crash).

[ ] Mac package script
   Steps:
     a. From project root: python scripts/packageMac.py --skip-venv
     b. dist/DataDoctor-macOS-YYYYMMDD.zip contains Data Doctor.command + pythonFiles/
        + updates/ + applyUpdate.sh + pythonFiles/certs/
     c. On a Mac: unzip, double-click Data Doctor.command (needs Python 3.13 / venv).
     d. Optional on Mac only: python scripts/packageMac.py --app → .app via PyInstaller.


Confirmed
--------------------------------------------------------------------------------

Deferred / known
--------------------------------------------------------------------------------
[!] AppImage applyUpdate path not wired the same as launcher packages yet