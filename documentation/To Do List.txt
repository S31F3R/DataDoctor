####### CURRENT TASKS #######
-Is there a way to make a GitHub issue programatically? I'm thinking during a crash that we catch or a button I add, maybe both. Have the ability to send the crash dump, meaning app.log, to GitHub
    -If this is possible, I would make it used during crashes and a new tool that allows a user to create an issue from this app
    -Don't act on this right away, it's currently up for dicussion and planning
-Need to start getting this ready for inital release. Need readme.md for github to be setup and need the wiki created for github also

####### CURRENT BUGS #######
-Ran applyUpdate.cmd and noticed the DataDoctor.py never got renamed to DataDoctor.pyw. I have an old .pyw and the update created the new .py
    -DataDoctor.py is supposed to get renamed to DataDoctor.pyw on the windows update. Not sure where the issue is but most likely in applyUpdate.py or applyUpdate.cmd
-Graph is ALWAYS rounding. If raw data is selected, the query returns raw data but the graph is still showing rounded
    -Data points within the graph need to show the raw data, but lets keep the axis clean. No need to have the axis show 7 decimals
-Not sure if this a Qt thing or something that can be fixed but minimizing some windows doesn't put it in the task bar. It minimizes to a very small title bar in the bottom right
    -This happens with Data Dictionary, so I'm guessing it's only child windows
    -This doesn't happen with the graph if I detatch it. Minimizing this one takes it to task bar under same icon as the main program, which is normal behavior
-When a query is finished, make sure to select Data Query tab. I was on graph tab and query was finished, I was still on graph tab
-Issue with delta. When raw data is selected in the options, I'm getting a delta when the numbers are exactly the same. I have a 9860.47950050633 on both databases (primary and secondary) with a delta of 0.000000000002
    -Found the issue. Delta's logic is before our decimal limiter or whatever. If I click the details, I can see primary database is actually 9860.479500506332 and secondary is 9860.47950050633, but in the Overlay Details it shows 9860.47950050633 for both
    -If I'm correct and we did put a limiter in for this reason, we need the delta logic to go off of that value. Leave the database values showing full percision like they are, like 9860.479500506332, just not in the overlay or delta check
        -This sounds confusing, may need to talk about it more

####### NEW FEATURES #######
-Create a plot tool that allows users to build different data plots and save that plot config as a quick look
    -This can be done with below parker/davis stretch also. Create a plot that finds travel times in a given stretch, based on user input. Below parker or davis would be saved as quick looks in this tool
    -Add a plot that graphs delta pair and overlays delta as a seperate line    
-Expand options to add more config options to the entire program
    -Maybe switch options to tree view or add vertical tabs
    -Aquarius will need an opiton to use credential account or entra account
        -Maybe default to entra, then if that doesn't exist then use credential if there? Need to research this
    -USBR will need:
        -Apply Overwrite Flag
-Add datatype as an option to add to labels
-Need a profile tab in options. Allow export/import of quickLooks, override system dark/light mode, etc.

####### QUERY ENHANCEMENTS MODIFICATIONS #######
-Aquarius daily TZ offset (UC 2300 vs expected 0000)
    -Data review tool shows 00:00; issue tied to location TZ vs UTC offset in query
     meaning UTC-6 queried as UTC-7 will display a daily at 2300 instead of 0000

####### WIKI IDEAS #######
-QAQC and how it works with Data Dictionary
-Aquarius link, what is required
-Explanation of BOP and EOP for USBR
-Overlay and delta explanation
-Overlay auto marked for edits/upload
-CSV dumps based on Data Query tab or SQL Build tab
-Diffrent quicklooks, how to save/use them
-AppData, what is stored and where
-dataDictionary valuePrecision and precisionOverride explanation
-Expladin how header labels from data dictionary are created 

####### GIT MODIFICATIONS BEFORE 3.0 RELASE #######
-Update readme
-Need documentation on different installers and pre req, maybe add in wiki or readme
-Provide links to stuff like requesting USGS API key, Reclamation website, USGS data browser, and documention on how to find USGS information to create dataID
-Document GitHub Releases: asset names (DataDoctor-Python.zip, DataDoctor-x86_64.AppImage),
  tag = vMAJOR.MINOR.PATCH or vX.Y.Z-rc.N, check Pre-release for beta

####### WORKFLOW (for Grok / Seifer) #######
-To Do List = backlog only (not currently being worked)
-When work starts on a bug: remove it from here, add it to documentation/test.txt with steps
-When Seifer confirms a test item: mark [x] in test.txt (and leave off this list)
-If a test fails: keep in test.txt with notes; do not put back on To Do until abandoned
-Clean up test.txt after confirmed [x] items (standard workflow): drop long steps for [x] into a short Confirmed list; keep retest items detailed
-Documentation: Grok may edit documentation/ as part of normal workflow (To Do, test.txt, notes, etc.)
-Commits:
    - Grok may auto-commit code paths (ui, core, scripts, README, requirements, sounds, icons, …)
    - Grok must NOT auto-commit documentation/ — Seifer commits those
    - secret.grok is gitignored; never commit it
