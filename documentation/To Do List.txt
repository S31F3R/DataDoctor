####### CURRENT TASKS #######
-Graph axis is working perfectly, it's going off of the rounded values. I want the actual data points and lines of the graph to go off of raw values. This will make it so the graph doesn't "flat line" if someone has rounding too "tight", not allowing to see change in data
    -Make sure to document this in the Wiki

####### CURRENT BUGS #######
-I copied a column with a formula, about 40 rows. I pasted that into another column, all 40 rows showed an edit and it was the values that pasted, not the formula. This is working as expected. I hit ctrl+z, it only undid the one at the bottom of the row. I hit ctrl+z, it did the one above that. If I hit ctrl+z enough, it eventually undoes all 40. Since this was a copy paste of all 40 in one go, it should have undid all 40, not one at a time
-Spacing for the color table in Appearance got screwed up last round, again. Now there is a big gap inbetween color theme/it's combo box and the title "Table colors", then there is yet another big gap until getting to the table
    -It looks like all spacing got messed up on Appearance tab, but I can't be too sure. It looks like there is more spacing between Retro Mode and the Color Theme title now and too much from that title to the combobox. Fix all of it
    -From what I can see, when this is fixed we should be able to make the height of the window smaller
-I copied out 3 decimal data and placed it in a safe spot so I can copy it back. I then changed all that data to 4 decimals by adding a 5 at the end. I loaded that back to the database then ran the query again. It all shows 3 decimals because of rounding rules but the raw data does show the 4th as a 5. When I paste that 3 decimal data back in, it's not showing up as a data edit because it matches the current rounding rules. When a user changes data, the marked for edit needs to go off of raw data
    -Be careful when fixing this. A double click with no change might trigger the "marked for edit" improperly again. That was a bug we had in the paste
    -Double clicking on a cell should actually show the raw data. Excel does this. You see the rounded data but if you double click for edit, you see the raw. This might help fix this logic if we add that in
    -Filling primary with secondary also needs to take the raw version of secondary and put it into primary. I'm assuming it's not like that already

####### 3.4.0 FEATURES #######
-Create a plot tool that allows users to build different data plots and save that plot config as a quick look
    -This can be done with below parker/davis stretch also. Create a plot that finds travel times in a given stretch, based on user input. Below parker or davis would be saved as quick looks in this tool
    -Add a plot that graphs delta pair and 4overlays delta as a seperate line
-Aquarius Options: credential account vs Entra (research; maybe Entra first, then credential)
-Create a new tool called SQL Manager
    -Add SQL Developer type tree view once logged in. It needs to have the same options SQL Devloper does, showing the different users, different groups, right click options to unlock accounts, etc. This will be big and I'll document more features
