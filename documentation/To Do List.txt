####### CURRENT BUGS #######
-Meta data has a bug: when clicking on a HDB tab it adds a 3rd column to the table. once this is done, if Overlay Details tab is clicked, it has that extra column also
    -If clicking on an HDB meta data tab then to a USGS-NWIS details tab, the extra column goes away. This was only tested on old USGS-NWIS query where meta data is blank
    -This bug doesn't always happen. Haven't figured out why it happens sometimes and other times it doesn't
    -This bug might only be tied to USGS data being in the context menu as overlayed. Got this idea from working on the USGS meta data bug documented below
-Meta data from context menu should only show 16 rows under the header. Anymore than that a scroll bar needs to be added, using the same color theme as the rest of the program
    -Manually sizing this does allow scroll but no scroll bar currently shows up
-Meta data bug with USGS data. Some rows are duplicated. Parameter Code, Statistic ID, Time Series ID, Monitoring Location ID are all in there twice. There might be more
-USGS meta data doesn't work if only USGS data is queried. Added HDB data, still didn't work but HDB data did have meta data
    -Validated USGS meta data only shows up when it's overlayed with data that context menu works on
    -Works if 2 USGS data are queried and overlayed
-thenetpi.jpg needs to be mvoed to ui>icons folder and renamed to pi.jpg
-Very large data queries seem to be getting stuck on "Building table...". Let it run for ~10 minutes but it never completed
    -Even if this isn't broke and would have completed after 30 minutes, need to look at a multi thread solution. Possibly organize the data first then place into table
-Does USGS only give instant and daily? If not, need to add in the rest of the queries for monthly, yearly, etc. This needs to match what the program can currently query
-Update tab index on all windows
-HDB headers are using commonName only, needs to be commonName - datatype
-Save quick look dialog needs to be wider by about 60px
-Query window needs to be wider. Currently can't fit new USGS query with UID
-Turning on or off retro mode and program restarting doesn't work on Windows, works on Linux
-When using raw data option, values that are .000000000X different are showing a delta that's using scientific notation. This needs to be fixed
-Need to catch "wrong user/password" for HDB login so multiple attempts don't lock the account
-Figure out how to change taskbar icon
-Text is slightly too big on Windows. Buttons in Query are truncated
    -There looks like there is extra padding above and under the text also. Not sure if a simple lower by 1 size will fix this
    -This might be tied to certain controls. THe SQL Query Builder area isn't impacted, either is the table output from the query, but the SQL quicklooks to the right are
-HDB data needs to bypass QAQC coloring when there is a value in rbase and not in interval. Use the blue box when data is missing, but place black text with the rbase value
    -Code currently works, just doesn't do the coloring properly

####### SQL QUERY BUILDER MODIFICATIONS #######
-Locks up ui when running query. Make it use seperate thread
-Export CSV needs to work based on what tab is selected

####### NEW FEATURES #######
-Create a plot tool that allows users to build different data plots and save that plot config as a quick look
    -This can be done with below parker/davis stretch also. Create a plot that finds travel times in a given stretch, based on user input. Below parker or davis would be saved as quick looks in this tool
    -Add a plot that graphs delta pair and overlays delta as a seperate line
-Add a log viewer with same type of formatting you get from reading .log files in Kate (WARN orange text, ERROR red text, etc)
    -Include a way to open/view app.log from inside the app (no hunting config paths)
-Add a button to see table with rounding rules based on data type (need table created first)

####### LOGGING ENHANCEMENTS #######
-See if there is a way to get trace errors to show up in logs
-See if there is a way to get QT_LOGGING_RULES to show up in logs

####### MAIN INTERFACE MODIFICATIONS #######
-Stress relief button that makes screaming goat sounds
-Handle on data dictionary too small, make it more bigger

####### QUERY ENHANCEMENTS MODIFICATIONS #######
-Formatting rules need to come from a file in the program, generated by the rules in Aquarius
    -Set to 2 decimals for now
    -Add this as a table in the sqllite database
    -Look into Aquarius query to pull value percision each query to get fresh rules
        -/AQUARIUS/Publish/v2/GetParameterList?format=json
    -Need to combine aquarius parameters to HDB data types for formatting rules
-modify and test for day, month, year, and water year queries
    -Aquarius daily from UC is comingin as 2300, that's whack
    -Data review tool shows 00:00, so my query might be whack
    -Issue is tied to how Aquarius handles different timezones. Daily data is getting offset based on the location timezone and the UTC offset sent in query
     meaning UTC-6 queried as UTC-7 will display a daily at 2300 instead of 0000
-Max/min context menu on headers need to show timestamp for max/min value
-Aquarius meta data needs to show Aquarius label as first item. Lable currently isn't setup

####### GIT MODIFICATIONS BEFORE 3.0 RELASE #######
-Update readme
-Need documentation on different installers and pre req, maybe add in wiki or readme
-Provide links to stuff like requesting USGS API key, Reclamation website, USGS data browser, and documention on how to find USGS information to create dataID
