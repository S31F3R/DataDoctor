####### CURRENT TASKS #######
-Import of sql quick looks need to put them in the Uncagtorized section

####### CURRENT BUGS #######
-Windows System Default for appearance isn't setting properly. Dark mode system setting is setting light
    -Manually setting Dark mode works, it's just the system setting that isn't properly using system on Windows

####### NEW FEATURES #######
-Create a plot tool that allows users to build different data plots and save that plot config as a quick look
    -This can be done with below parker/davis stretch also. Create a plot that finds travel times in a given stretch, based on user input. Below parker or davis would be saved as quick looks in this tool
    -Add a plot that graphs delta pair and overlays delta as a seperate line
-Aquarius Options: credential account vs Entra (research; maybe Entra first, then credential)
-Profile in Options: export/import Quick Looks
-Get OAuth App for GitHub stuffs

####### QUERY ENHANCEMENTS MODIFICATIONS #######
-Aquarius daily TZ offset (UC 2300 vs expected 0000)
    -Data review tool shows 00:00; issue tied to location TZ vs UTC offset in query
     meaning UTC-6 queried as UTC-7 will display a daily at 2300 instead of 0000
