####### CURRENT TASKS #######
-Update message for Windows is too "wordy". Don't need to say anything after "Restart Data Doctor to apply the update"
    -Since applyUpdate.cmd can start up Data Doctor.exe, could we add a message box with a button that says "Restart"
        -Restart could close the program but launch applyUpdate.cmd
        -If this can be done, change the message to "Hit restart to update or close window to restart later" or something like that

####### CURRENT BUGS #######

####### 3.2.0 FEATURES #######
-Create a plot tool that allows users to build different data plots and save that plot config as a quick look
    -This can be done with below parker/davis stretch also. Create a plot that finds travel times in a given stretch, based on user input. Below parker or davis would be saved as quick looks in this tool
    -Add a plot that graphs delta pair and 4overlays delta as a seperate line
-Aquarius Options: credential account vs Entra (research; maybe Entra first, then credential)
-Get OAuth App for GitHub stuffs