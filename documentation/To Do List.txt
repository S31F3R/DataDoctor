####### CURRENT TASKS #######\
-The filters work great but maybe it was a bad idea to have siteID as a combo box. Lets make it a user input instead, so they need to type in what they want to filter by

####### CURRENT BUGS #######
-Combo box in the filter has be clicked twice before it expands the list of items
-Still having spacing issue in the date/time column on the query table. Look in documentation for BAD.png and GOOD.png. After you review, remove those 2 .png files. BAD is the spacing that happens with retro, GOOD is the spacing without retro. See the "padding" before the date and after the time? That's what I'm trying to get rid of but nothing we've done is fixing that

####### 3.3.0 FEATURES #######
-Create a plot tool that allows users to build different data plots and save that plot config as a quick look
    -This can be done with below parker/davis stretch also. Create a plot that finds travel times in a given stretch, based on user input. Below parker or davis would be saved as quick looks in this tool
    -Add a plot that graphs delta pair and 4overlays delta as a seperate line
-Aquarius Options: credential account vs Entra (research; maybe Entra first, then credential)
-Get OAuth App for GitHub stuffs
