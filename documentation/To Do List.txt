####### CURRENT TASKS #######
-Linux appimage is too wordy on New Version is Available. Make it more standard like the Windows version except the Linux specific pieces. No need to go into detail on what the updater is doing
    -Same with restart message. This actually might be fixed and maybe the one above. It might be fixed in the version I'm going to but verify. What I'm seeing on the restart message is "replace data doctor.appimage now and restart data doctor? the file keeps its current name. choose later to apply on next launch." If this is different already, then leave it
    -If the above isn't different but the New Version Available isn't, make sure to fix that one

####### CURRENT BUGS #######
-Not sure if this is Windows only but the behavior needs to be this way for all of them. On Windows, there was an update so I selected download. After the download was done, I didn't want to restart so I hit later. Behind that was the original update window with Download or Later. This window should have gone away when I hit Download or Later. I had to click Later twice with what I didn
    -If download is hit, close that window and show the download progress bar. If after that is done and Later is hit, just close that window back to main program
-When clicking the X on the graph legend to hide a data set, the [] collapse. Lets make it so they are open, like it can have something in there, so like this [ ]
-I used an overlay query that had HDB and Aquarius data on public. This was a test to see if I could run this query and have it ignore the Aquarius query dataID since we can't query Aquarius on public. So the query was HDB, AQ, HDB, AQ, etc in that order for dataID. THe overlay skipped AQ and just used the next HDB, which is wrong. Need to make it so overlay doesn't go through if it can't query it's "paired data set" from the query list. it should have just done a normal non overlay query for that pair
    -Some of the datasets in this query were HDB and USGS-NWIS. Those can still be overlayed. I'm just looking to fix combinations are not supposed to work, so in this example Aquarius

####### 3.4.0 FEATURES #######
-Create a plot tool that allows users to build different data plots and save that plot config as a quick look
    -This can be done with below parker/davis stretch also. Create a plot that finds travel times in a given stretch, based on user input. Below parker or davis would be saved as quick looks in this tool
    -Add a plot that graphs delta pair and 4overlays delta as a seperate line
-Aquarius Options: credential account vs Entra (research; maybe Entra first, then credential)
-Create a new tool called SQL Manager
    -Add SQL Developer type tree view once logged in. It needs to have the same options SQL Devloper does, showing the different users, different groups, right click options to unlock accounts, etc. This will be big and I'll document more features
