####### CURRENT TASKS #######
-Update message for Windows is too "wordy". Don't need to say anything after "Restart Data Doctor to apply the update"
    -Since applyUpdate.cmd can start up Data Doctor.exe, could we add a message box with a button that says "Restart"
        -Restart could close the program but launch applyUpdate.cmd
        -If this can be done, change the message to "Hit restart to update or close window to restart later" or something like that
    -I'm thinking this is on the Windws update for the launcher. I updated another computer and it did the normal update for 3.1.1, then when that was done and I opened it again, it did another for the Windows package for 3.1.1. I had to install this manually running applyUpdate.cmd
        -This seemed like extra steps but not sure if there was a way around this. Maybe if the applyUpdate.cmd ran and we added another download to get the proper version if there is a launcher up date? User already confirmed it was ok to download the update. At this point of the program, it would be a good spot to have the script download the right version then unpackage and install it
-The first time clicking Graph, Options, or About take a while for it to load. If this is because of some type of UI or code generation, how about we do it on a seperate thread after the main app is opened. That way it doesn't slow down the program and makes those windows launch quicker when the user is ready to use them
-The current retro font has a lot of flaws. Lets look for a different retro font, see if it works better
-Add a undo/redo using ctrl+z and ctrl+y
    -This will most likely be slightly different per tool but I'd like it for the table and SQL
        -No need for context menu or helpers. This is for normal computer users who are used to the ctrls already. Can still have it mentioned in the wiki for table and SQL sections
-Give the abilith to right click a column header, then an option to insert right or insert left. This will be a blank column
    -Make sure this doesn't impact queries. User should be able to refresh the query and have this column stay. This will be used for users, such as formulas or just pasting in other data
        -This means any formulas they put into that column need to update after refresh
        -This also means this column needs to follow the rest of the tables sorting logic so pay close attention to this
        -Give the option to rename the header in this column. This should be a right click context menu option
            -It should also have show details, which would just be Max, Min, means
            -This needs to be able to be graphed as well
        -Make sure export to .csv logic will work with this
-Give the option to drag and move columns
    -When a column is moved, make sure to also move it in the query list, meaning move the dataID in the list to mimic the new order
    -This needs to work with Overlay
        -Make sure if a column is moved, the query list moves the overlay pair
    -If a column is moved and Deltas is checked, the delta column needs to move with it. They basicall need to be "locked" together
    -If a custom column was created, it can be moved freely and independantly, just like a normal data column. This could have been implied but wanted to make sure it's not locked like a delta column
-Add a context menu to table header for Overlay queries. Call it "Swap Primary/Secondary". This will change which dataID is considered primary and secondary for that query
    -Make this swap them in their query also
    -If deltas is checked, make sure to run the delta check to reflect the swap change. It should be just changing sign and color of the delta logic
-Give the option to rename ANY header column. Renames should only handle the commonName, meaning do not give the user the option to rename the dataType portion
    -If a header is renamed, we need to keep this tracked, so on a new query we can prompt the user and say "Headers renamed, save/update Data Dictionary as Common Name?". This will either update the Data Dictionary or save a new item in the Data Dictionary
        -If it's a new item, fill in the dataID from the query, fill in the database, and the commonName
            -On dataID, use the time_series_id for USGS, for USBR just the SDID without the MRID piece (parse by - and take spot 0), AQUARIUS is the UID
            -If it's an AQUARIUS query, fill in the datatype with the parameter we get in the meta data for Aquarius
            -If it's USGS, fill in the siteID with the 2nd part of Monitoring Location ID (parse by - and take spot 1), fill in siteName with the Site Name, and datatype with Parameter Name. ALl of which are in the meta data
-When Overlay is seclected and there is a difference between primary and secondary, add a context menu option that says "Update from secondary". This will update the primary value with what's in secondary and mark the edit change
    -Make sure this doesn't get past the code that blocks updating USGS or AQUARIUS data
    -Only update ones that are flagged as different. Do not touch values that are identical
    -Should be able to be used on a single cell or on all highlighted. Since the code is only using secondary to override primary when the difference is > 0, should be pretty safe
-Need to make Overlay queries smarter. When raw data is unselected, need to format the values based on the formatting rules for the Primary. If the primary formats to DEC(3) and secondary DEC(2), format the secondary to DEC(3)
-The header in the graph legend that says "Click to show/hide" isn't needed, remove it

####### CURRENT BUGS #######
-When hitting the up or down arrows in query window, it scrolls the query list. Keep the scroll position where it was at, unless the arrows are moving outside of the current view range
-Double clicking a cell but not making any edits makes the cell background black
    -On a cell with white text, meaning no QAQC flagged data but QAQC on, the text gets turned black also
        -I'm on dark mode and the text was white
    -On a cell with red text, the text stays red but background turns black
    -It looks like it only changes "default" formatting of cells. If the cell background or text color was changed due to QAQC, it's not impacted by this bug
    -If I double click back into the cell, I can see text again so the data is there, just formatting messed up
-In Light mode, the SQL query text can't be read. It's barley visible, looks like it's using a light grey color
    -The result table has a black background on both scroll bars
    -Black background for behind the snippet buttons and the comobobox
    -THe issues with the colors might be something that is had coded. The switch between light and dark mode is just supposed to switch the "singals" coming from the system instead of changing colors for every control manually or in code
    -None of these issues exist if data doctor is closed then reopened
        -On this same session, switched back to Dark mode and the issues are reversed. SO it looks like the switch isn't applying to all controls correctly
        -Running same query again doesn't fix the text or controls
        -Opening new query window fixes text and result controls
        -Hiding snippet window then unhiding doesn't fix
        -Closing SQL tab and reopening it doesn't fix any of the conrols
-Just graphed data with missing data. Instead of the graph showing the missing data, it drew a straight line between the last good reading and the first reading that came back in. Need the grpah to not draw that so it's easier to see the gap

####### 3.3.0 FEATURES #######
-Create a plot tool that allows users to build different data plots and save that plot config as a quick look
    -This can be done with below parker/davis stretch also. Create a plot that finds travel times in a given stretch, based on user input. Below parker or davis would be saved as quick looks in this tool
    -Add a plot that graphs delta pair and 4overlays delta as a seperate line
-Aquarius Options: credential account vs Entra (research; maybe Entra first, then credential)
-Get OAuth App for GitHub stuffs