# Data Doctor 3.1.1

Channel: published

## Changes

- Windows silent launch: do not construct About (QtMultimedia / pygame) at startup; log launcher + applyUpdate + python heartbeats to AppData app.log; faulthandler before Qt; software Qt on Windows VMs
- Windows launcher (app.vb): append to app.log and wait ~8s if pythonw dies immediately (rebuild Data Doctor.exe on Windows)

## Notes

Windows: already on python-embed → `DataDoctor-Python-*.zip` + `applyUpdate.cmd`. Coming from 3.0.x → `DataDoctor-Windows-*.zip`, close the app, then `applyUpdate.cmd` (replaces the launcher).
