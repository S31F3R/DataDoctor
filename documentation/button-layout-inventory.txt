Button / control layout inventory
=================================
Updated: 2026-07-24
Code map: core/Utils.py → CONTROL_LAYOUTS + applyModeControlLayouts()

Modes
-----
  default = Noto Sans (retro OFF) — full key set restores all moved controls
  retro   = Press Start — RESPONSE pass 10 APPLIED


DEFAULT (Noto) — CURRENT in code
--------------------------------
winMain
  btnRefresh            (22, 6, 32, 32)     Linux; Windows y=8 FINAL
  btnUndo               (70, 6, 32, 32)
winOptions
  chkbRawData           (74, 60)
  chkbQAQC              (156, 90)
  chkbRetroMode         (88, 120)
  chkbDebug             (96, 150)
  chkbEnableSQL         (128, 180)
  rbBOP                 (210, 0) RESPONSE: -4 x
  rbEOP                 (350, 0)
winQuery
  btnDataIdInfo         (376, 5)
  btnIntervalInfo       (100, 76)
  btnQueryOptionsInfo   (110, 401)
  chkbOverlay           (150, 424)


RETRO (Press Start) — CURRENT in code (pass 10)
----------------------------------------------
winMain
  btnRefresh            (56, 8)       y corrected -4
  btnUndo               (106, 8)      y corrected -4
winOptions
  chkbRawData           (108, 60)
  chkbQAQC              (261, 88)     
  chkbRetroMode         (130, 119)    
  chkbDebug             (130, 149)
  chkbEnableSQL         (206, 179)
  rbBOP                 (193, 0)      -6 x (from 199)
  rbEOP                 (350, 0)
winQuery
  btnDataIdInfo         (406, 5)
  btnIntervalInfo       (164, 76)
  btnQueryOptionsInfo   (164, 401)
  chkbOverlay           (162, 424)

Fonts / table / tabs (mode-aware on restart)
--------------------------------------------
  btnAddQuery + btnQuery: button role + 1pt
  RETRO_SMALL_FONT_CONTROLS @ 6pt (retro only)
  Windows+retro only −1pt: btnLoadQuickLook, btnClearQuery, btnSaveQuickLook,
    btnDeleteQuickLook (same row), listQueryList
  Headers: part1\\n\\npart2 (retro only)
  Header minHeight set in retro, cleared (0) in default
  Table: no QSS pad; original column width math
  Tabs retro: bg #00FF00 (scrollbar green), text black — removed when retro off


Toggle test (Linux then Windows)
--------------------------------
  1. Retro ON → restart → positions/fonts/table header height as retro
  2. Retro OFF → restart → Noto positions (all keys above), normal fonts, header minHeight 0
  3. Open Options + Query after each restart; check Refresh/Undo, checks, info icons, BOP/EOP, Overlay
  4. Run a query: two-line headers with blank between; column widths sensible


(Add new RESPONSE lines; Grok applies into CONTROL_LAYOUTS['retro'])
